{
  "_doc": "Phase 6.3 partition manifest. Assigns every skill + rule to exactly ONE logical module. Seam: core = workflow engine + CROSS-CUTTING domain skills (used across stacks); spring/messaging/quality = independent domain packs. This is a LOGICAL partition (CC discovers skills only at flat skills/<name>/, and CC plugins are independent units with NO cross-plugin references or dependency resolution — feature request #9444 — so physical 4-plugin distribution is BLOCKED-ON-PLATFORM; see README.md). tests/static/module-coupling.sh proves the partition holds: no pack->pack reference edge. A future split (post-#9444) consumes this manifest directly.",
  "modules": {
    "core": {
      "role": "workflow engine + cross-cutting domain skills (every install needs these)",
      "skills": [
        "route", "brainstorm", "spec", "plan", "build", "verify-review", "learn",
        "init", "discover", "using-claudehut", "write-skill", "systematic-debug",
        "reuse-scan", "tdd-cycle",
        "lombok", "jackson", "mapstruct", "testcontainers", "wiremock-stub"
      ],
      "rules": [
        "architecture/adr-format", "architecture/cqrs", "architecture/ddd",
        "architecture/hexagonal", "architecture/package-layout",
        "coding/exception", "coding/immutability", "coding/logging-mdc",
        "coding/naming", "coding/null-safety", "coding/optional-stream",
        "coding/records-sealed",
        "framework/jackson", "framework/lombok-annotations", "framework/lombok-builder",
        "framework/lombok-jpa-safety", "framework/mapstruct",
        "testing/coverage", "testing/given-when-then", "testing/junit5",
        "testing/mockito", "testing/tdd-cycle", "testing/testcontainers", "testing/wiremock"
      ]
    },
    "spring": {
      "role": "Spring framework data/web stack",
      "skills": ["spring-mvc", "spring-webflux", "jpa-hibernate", "r2dbc", "flyway-migration", "redis-cache"],
      "rules": [
        "framework/spring-mvc", "framework/webflux", "framework/jpa", "framework/r2dbc",
        "framework/flyway-naming", "framework/redis", "framework/migration-safety",
        "performance/backpressure", "performance/caching", "performance/connection-pool",
        "performance/indexing", "performance/n-plus-one",
        "security/spring-security", "security/actuator",
        "testing/stepverifier"
      ]
    },
    "messaging": {
      "role": "message brokers",
      "skills": ["kafka-consumer", "kafka-producer", "nats", "rabbitmq"],
      "rules": ["framework/kafka-consumer", "framework/kafka-producer", "framework/nats", "framework/rabbitmq"]
    },
    "quality": {
      "role": "security + architecture quality gates",
      "skills": ["owasp-scan", "arch-unit-check"],
      "rules": ["security/owasp-top10", "security/input-validation", "security/deserialization", "security/secret-mgmt"]
    }
  }
}
