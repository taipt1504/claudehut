<!-- generated by claudehut-init from claudehut plugin templates/PROJECT.md.tmpl — safe to edit -->
# Project: {{PROJECT_NAME}}

- Build: {{BUILD_TOOL}} · verify: `{{VERIFY_COMMAND}}`
- Java {{JAVA_VERSION}}, {{SPRING_MODULES}} ({{REACTIVE_OR_MVC}})
- ORM / data: {{ORM}} ({{DATABASE}})
- Messaging: {{MESSAGING}} · Cache: {{CACHE}}
- Base package: {{BASE_PACKAGE}}
- Layers: {{LAYER_CONVENTION}}
- Entry points: {{ENTRY_POINTS}}
- Test strategy: {{TEST_STRATEGY}}
- Architecture style: {{ARCH}}
<!-- ddd | hexagonal | cqrs | none — EDIT THE LINE ABOVE. claudehut-init reads it back on every run and
     emits the matching .claude/rules/architecture/ rule. Declared, never guessed: the three styles are
     mutually exclusive, so guessing one would install rules that contradict the other two. -->

