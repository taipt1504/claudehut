<!-- generated by claudehut-init from claudehut plugin templates/architecture.md.tmpl — safe to edit -->
# Architecture — {{PROJECT_NAME}}

Detailed package/layer map. **On-demand** (not @import-ed) — read when exploring structure.

## Modules
{{MODULE_LIST}}

## Package layout (base: {{BASE_PACKAGE}})
```
{{PACKAGE_TREE}}
```

## Dependency direction
{{DEPENDENCY_DIRECTION}}
(e.g. web → service → domain → persistence; domain depends on nothing)

## Cross-cutting
- Transactions: {{TX_STRATEGY}}
- Error mapping: {{ERROR_MAPPING}}
- Messaging topology: {{MESSAGING_TOPOLOGY}}
