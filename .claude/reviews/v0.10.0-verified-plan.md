# ClaudeHut v0.10.0 — Verified Upgrade Plan

Synthesis lead: every claudehut claim below was re-derived at `HEAD=4b3862b` by opening the file or running the script. Every platform claim was re-fetched this session (`hooks.md`, `sub-agents.md`, `skills.md`, `plugins-reference.md`) and quoted from raw markdown, not a summariser. Where a proposal and its verifier disagreed, I opened the file and say who was right.

---

## 0. Where v0.9.2 left us

### The eval baseline is 406, not 410 — the brief is stale

I ran all eight named suites individually at HEAD:

| suite | passed | failed |
|---|---|---|
| `evals/conformance.sh` | 207 | 2 |
| `evals/gate-tests.sh` | 77 | 0 |
| `evals/init-tests.sh` | 42 | 0 |
| `evals/reference-check.sh` | 8 | 0 |
| `evals/merge-learnings-tests.sh` | 32 | 0 |
| `evals/ranker-tests.sh` | 8 | 0 |
| `evals/worktree-tests.sh` | 18 | 0 |
| `evals/artifact-oracle-tests.sh` | 14 | 0 |
| **total** | **406** | **2** |

Both failures come from the untracked `skills/fable5-thinking/` stub (`git status --porcelain` shows only `?? skills/fable5-thinking/`), exactly as the brief says — but the passing total is 406. **Use 406/2, not 410/2.** Anything quoting 410 will look like it broke four assertions. Two independent researchers and I measured 406; the 4-test gap is unexplained and I did not reconcile it.

Second baseline fact the brief does not mention: **CI runs only six of the eight suites.** `.github/workflows/ci.yml:59-77` invokes conformance, gate-tests, init-tests, ranker-tests, merge-learnings-tests, artifact-oracle-tests and `scripts/lint-prompt-length.sh`. `reference-check.sh` (8) and `worktree-tests.sh` (18) never run in CI — 26 assertions, 6.4% of the suite, with zero regression coverage, including the one guarding `bin/claudehut-worktree`, a concurrency surface.

### What v0.9.2 already banked — do not double-count these

- **Model re-tiering** (`52b9447`): changes price-per-token, not token count. Not measurable in bytes.
- **The per-Stop haiku agent hook is gone** from `hooks/hooks.json` — I read all 79 lines; the registered events are SessionStart, UserPromptSubmit, UserPromptExpansion, PreToolUse (×2), PostToolUse, PostToolUseFailure, Stop, SubagentStop, PreCompact. This removed a *model call per Stop*, likely the largest real saving in the release even though its byte delta is small.
- **SessionStart digest swap**: `skills/claudehut-workflow/references/digest.md` is 4,294 B vs the 10,191 B `SKILL.md` — a real 5,897 B saving per session-start event. But the *assembled* `additionalContext` is what matters: I ran the real hook with `{"session_id":"s1","source":"compact"}` and measured **4,374 B** emitted (digest + learnings + the understand-anything line). A prior researcher's 7,704 B figure was measured against a 20-record synthetic learnings store. Set v0.10 targets against what you measure locally, not against either number.
- **Delta-only `inject-phase.sh`** and `--max-len`/`--exclude` on `inject-learnings.sh`.
- **Tier-aware auditor selection + 2-round caps** in `skills/review/SKILL.md:63-69,141-143`.

### Top remaining recurring costs (measured at HEAD)

1. **`skills/implement/SKILL.md` = 16,213 B, preloaded whole into every implementer.** `agents/claudehut-implementer.md:7-8` declares `skills:` / `- implement`, and [sub-agents.md:284](https://code.claude.com/docs/en/sub-agents) states: *"Skills to preload into the subagent's context at startup. The full skill content is injected, not only the description."* Lines 74-131 (**5,289 B measured**) are pure main-thread orchestration — the phase walk, worktree reconcile, the native task mirror — and `agents/claudehut-implementer.md:6` is `tools: Read, Edit, Write, Bash, Grep, Glob`: no Agent tool, no task tools. That text is dead weight in every implementer dispatch, and `skills/implement/SKILL.md:125` says so itself (*"Subagents have no task tools — they cannot update the list"*).
2. **`skills/review/references/review-rigor.md` = 2,911 B, hand-pasted per auditor per round.** `skills/review/SKILL.md:81` requires it *verbatim* in every code-review dispatch prompt; :63-69 selects up to 6 auditors; :141-143 permits 2 fix→re-spawn rounds.
3. **13 rule templates glob `**/*.java`, totalling 40,350 B, and none carries a `stack:` key** — so `bin/claudehut-init:127-130` skips the tag gate and :138 `cp -f`s all 13 into every project unconditionally. Three are *mutually exclusive architecture styles*: `templates/rules/architecture/ddd.md` 3,328 + `hexagonal.md` 3,811 + `cqrs.md` 2,246 = **9,385 B** of simultaneously-loaded contradictory guidance. `evals/EVAL-REPORT.md:123-134` already measured that a `paths:` rule fires 3/3 on **edit** of a matching file (0/3 on create), so these do load.
4. **`skills/review/SKILL.md:85` calls `inject-learnings.sh --top 8` with no `--max-len`,** and `scripts/inject-learnings.sh:14` defaults `MAXLEN=0` (uncapped). `bootstrap.sh:68` and `inject-phase.sh:56` both pass `--max-len 200`; the review dispatch — the one place the payload is multiplied by up to 6 auditors — is the sole uncapped caller.
5. **2,137 B of markdown-table padding in `agents/claudehut-brainstormer.md`, invisible to the linter.** `scripts/lint-prompt-length.sh:29` measures `grep -c ''` (lines). Commit `a458e6d`, titled *"shrink the always-loaded agent corpus"*, grew that file by 1,933 B while its line count *fell* by 9. I re-measured: squeezing interior spaces on `^\s*\|` rows recovers 2,164 B corpus-wide, of which 2,137 B is that one file and no other file recovers more than 18 B.

### Two reproduced defects that outrank every optimisation

- **`bin/claudehut-state` loses updates under concurrency.** I raced `set-complexity small` against `set-profile feature` (orthogonal fields, no reset interaction) 15 times from a common base: **15/15 lost updates.** The read is at `bin/claudehut-state:49-50`, the write at :338-343 — atomic `mv` protects readers, not the read-modify-write. The file claims to be "the SOLE writer" at :2-3, but `scripts/record-skill.sh:21-22` and `scripts/record-skill-expansion.sh:41-42` both invoke it from hooks. *Note: the probe used in one proposal (`set-phase brainstorm` vs `mark-skill implement`) proves nothing — `bin/claudehut-state:135` deliberately resets `implement_skill_ok=false` on `set-phase discover|brainstorm`, so that outcome is the correct serialisation.*
- **`scripts/gate-write.sh:38-41` exempts on un-normalised substrings.** Reproduced against the real script with a real state file: `$PROJECT_DIR/src/main/java/com/acme/test/Pay.java` → **ALLOW** (a production class in a Java package named `test`), and `/etc/.claude/claudehut/../../../src/main/java/Evil.java` → **ALLOW**. Meanwhile `exists_canon()` at :60-65 *does* normalise. The two disagree inside one file.

---

## 1. The plan — ordered, shippable increments

**Ordering rationale (overriding both the cost-first and capability-first proposals).** I sequence risk-first. Item 1 is a reproduced data-corruption bug in the file every gate reads; items landing on top of a store that can silently lose writes have untrustworthy tests. Item 2 is two reproduced fail-open bypasses of the plugin's only hard action gate. Only then do the cost and capability items land.

---

### 0. PREREQUISITE — resolve `skills/fable5-thinking/`

**Change.** Move the untracked stub out of `skills/` (e.g. to `~/.claude/skills/`), or finish it with a non-empty ` ```mermaid ` block and bump the C1 pin to 11.

**Files.** `skills/fable5-thinking/` (untracked); optionally `evals/conformance.sh:18-19`.

**Why.** `evals/conformance.sh:18-19` is `SK=$(ls -1d "$ROOT"/skills/*/ | wc -l)` hard-asserted `= 10`, and the mermaid loop at `:540-562` iterates the same glob. These two produce *both* current failures. Until it is resolved, every "how to verify" command below prints a red baseline and no reviewer can distinguish a new break from the stub.

**Do NOT** make C1 count only git-tracked directories — that couples a hermetic eval to git state and moves the failure later, since a contributor's genuinely-new skill is untracked before commit.

**Effort** XS. **Risk** none. **Verify** `bash evals/conformance.sh | tail -1` → `209 passed, 0 failed`, total **408/0**. Every item below quotes deltas against **408/0** post-prerequisite (406/2 pre).

---

### 1. Lock the state writer's full read-modify-write

**Change.** Extract the lock helper at `scripts/merge-learnings.sh:53-84` (flock preferred :66-67, atomic-mkdir fallback :75, 30 s stale steal :77, wall-clock fail-open :80) into `scripts/lib/lock.sh`. Source it from `bin/claudehut-state` and acquire immediately after argument parsing, releasing on `EXIT` — wrapping the whole read-modify-write, not just the rename.

**Files.** `bin/claudehut-state`, `scripts/merge-learnings.sh`, `scripts/lib/lock.sh` (new), `evals/gate-tests.sh`.

**Why.** Reproduced 15/15 (§0). `bin/claudehut-state:49-50` reads, :338-343 writes; two concurrent invocations share a base and the second `mv` discards the first. Three callers exist: the binary itself, `scripts/record-skill.sh:21-22` (`hooks/hooks.json` PreToolUse matcher `Skill`) and `scripts/record-skill-expansion.sh:41-42` (`hooks/hooks.json` UserPromptExpansion matcher `implement|discover|brainstorm`). Commit `963cee8` is the precedent that this class bites in CI.

**Effect.** Data integrity, no token effect. When `mark-skill implement` is the loser, `implement_skill_ok` reverts to `false` and the next production write is denied at `scripts/gate-write.sh:96` — whose text (*"If you DID just invoke it and still see this, the recorder hook failed"*) reads like this has been observed and misattributed.

**Effort** M. **Risk** low. A 1–2 s wait cap is defensible on user-perceived latency, **not** on hook timeout: [hooks.md](https://code.claude.com/docs/en/hooks) sets the command-hook default at 600 s and lowers it only for UserPromptSubmit (30) and MessageDisplay (10) — PreToolUse is not lowered, so the "hook gets killed" rationale in one proposal is false. Be honest in the changelog: a fail-open lock **narrows** the window, it does not close it. Platform note: CI is ubuntu-latest (flock branch); macOS has no `flock` and runs the mkdir branch — the same test validates a different code path per platform.

**Verify.** Add a stress case to `evals/gate-tests.sh` racing **orthogonal** fields (`set-complexity` vs `set-profile`). A test using `set-phase` vs `mark-skill` passes on the broken binary too — zero discriminating power. Baseline 408 → 408+N.

---

### 2. Normalise the write gate's exemption — one root cause, three misbehaviours

**Change.** In `scripts/gate-write.sh`, normalise to an absolute path with a **pure-string** `..`/`.` collapse, scope the `.claude/claudehut/` exemption to paths under `$PROJECT_DIR`, and anchor the test pattern to `*/src/test/*` instead of the bare `*/test/*`.

**Files.** `scripts/gate-write.sh`, `evals/gate-tests.sh` (**same commit**).

**Why.** Reproduced (§0): package-named-`test` prod file → ALLOW; traversal string → ALLOW. `gate-write.sh:38-41` matches raw substrings; `exists_canon()` at :60-65 normalises. Sibling `scripts/gate-done.sh` does *not* share the bug (it normalises at :56 and :87), so the blast radius is one file.

**Implementation constraints.** (a) The hook fires `PreToolUse` on files that do not exist yet — normalisation must be lexical, since macOS `realpath` without `-m` fails on nonexistent paths. (b) The script runs under `set -euo pipefail` (:8); a helper returning non-zero kills the hook, which emits nothing, which Claude Code treats as **allow** — a botched fix silently converts the gate into a no-op.

**Effort** M. **Risk** moderate, concentrated in the eval. A verifier ran the patched script against the full suite: **exactly one** existing assertion breaks — `evals/gate-tests.sh:221-222` (`P1-2: MultiEdit artifact path wrongly gated`, fixture `/p/.claude/claudehut/tasks/0001-slug/spec.md`), fixed by re-pointing at `$CLAUDE_PROJECT_DIR`. The proposal predicted `:57` would also break; it does not — and *that is a finding*: by line 57 the fixture already has reuse_scan+spec+plan+mark-skill set (lines 41-48), so the write is allowed by the full-tier rails regardless of the exemption. **`evals/gate-tests.sh:57` has never exercised the exemption it names.** Strengthen it by moving it before the artifacts are recorded.

**Scope honestly.** The traversal case sits inside the documented threat-model exclusion at `.claude/docs/design/06-hooks.md:166` (*"targets drift, not adversarial agents"*). The realistic defect is the package-named-`test` fail-open. The relative-path fail-*closed* case is likely unreachable — Claude Code's Write/Edit schema mandates an absolute `file_path` — so fix it (free) but do not claim a live deadlock.

**Verify.** Must include a sanity assertion that the gate **still denies** `$CLAUDE_PROJECT_DIR/src/main/java/A.java`; under `set -e` that is the only thing proving the gate did not become a no-op. Run `bash evals/gate-tests.sh` → 77+N passed, 0 failed.

---

### 3. Fix the plugin-scoped `agent_type` — the SubagentStop artifact gate has never fired

**Change.** One line after `scripts/verify-subagent.sh:20`: `agent="${agent##*:}"`.

**Files.** `scripts/verify-subagent.sh`, `evals/gate-tests.sh`, `evals/conformance.sh`.

**Why.** [hooks.md:2006](https://code.claude.com/docs/en/hooks): *"For subagents shipped by a plugin, the agent type is the plugin-scoped identifier such as `my-plugin:reviewer`, not the bare frontmatter name."* `scripts/verify-subagent.sh:20` reads `.agent_type` and its case arms at :24, :29, :34, :48 are bare names, falling through to a no-op `*)` at :65-67. Four artifact contracts the plugin believes it enforces — `reuse-scan.md`, `plan.md`, `plan-review.md`, `learn-candidates.jsonl` — are dead in production. `verify-subagent.sh` is the only consumer of `agent_type` in the repo.

**Effect.** Correctness restoration; arguably **negative** on tokens, because a gate that starts blocking costs re-dispatches it currently skips. That is the point.

**Effort** S. **Risk** real: this turns a dormant blocking hook live, so latent artifact-path mismatches start blocking. Bounded by the `stop_hook_active` fail-open cap at :18. **Land it alone**, so any new block is unambiguously attributable.

**Verify.** Because `${agent##*:}` accepts both shapes, all eight existing bare-name assertions (`evals/gate-tests.sh:201,203,204,208,210`; `evals/conformance.sh:427,430,432`) keep passing — a verifier confirmed 77/0 and 207/2 unchanged on a patched copy. **Add scoped-name twins** in the same commit, or CI proves only the synthetic shape. `evals/conformance.sh:512` (literal grep for `plan-review.md`) is untouched.

---

### 4. Cap the review learnings payload (two one-liners)

**Change.** (a) `scripts/inject-learnings.sh:14`: `MAXLEN=0` → `MAXLEN=200`. (b) `skills/review/SKILL.md:85`: add an explicit `--max-len 200` so the behaviour is visible at the call site.

**Files.** `scripts/inject-learnings.sh`, `skills/review/SKILL.md`.

**Why.** `bootstrap.sh:68` and `inject-phase.sh:56` already pass `--max-len 200`; `skills/review/SKILL.md:85` (`--top 8`, no cap) is the sole uncapped caller and the only one multiplied by up to 6 auditors × 2 rounds. A default flip makes a forgotten flag impossible. **This is also a hard prerequisite for item 7** — see the 10,000-character spill rule there.

**Effect.** Tail-risk cap, **unmeasured estimate** — worth ~0 on a store of crisp one-sentence records (`agents/claudehut-learner.md:55` asks for exactly that) and several KB per auditor on a verbose store.

**Effort** XS. **Risk** low. Truncation at 200 chars cuts mid-word (`inject-learnings.sh:69`); an auditor is told to add a coverage row per pitfall, so a fragment is worse than useless. **Consider 400 for the review path** and word-boundary truncation.

**Verify.** `bash evals/merge-learnings-tests.sh` and `bash evals/ranker-tests.sh` unchanged (32/0, 8/0). No conformance assertion touches this.

---

### 5. Split the implement skill — orchestration out of the per-implementer preload

**Change.** Move `skills/implement/SKILL.md:74-131` (**5,289 B measured**) to `skills/implement/references/orchestration.md`. Leave a short stub under the `:60` heading.

**Files.** `skills/implement/SKILL.md`, `skills/implement/references/orchestration.md`, `evals/conformance.sh`.

**Why.** §0 cost #1. The preload is verified eager (`sub-agents.md:284`), the implementer has no Agent or task tools (`agents/claudehut-implementer.md:6`), and the main thread reaches this text through the required skill rail at `scripts/gate-write.sh:94-97,110,124`.

**Extract 74-131, not 60-132.** Lines 68-72 are the fast-lane inline path and the `small`-tier mini-brainstorm — exactly what the main thread needs in the tiers that have **no** `plan.md`. Keeping them also preserves the `PHASE BY PHASE` heading at :60.

**Four pinned phrases, not two.** The stub must carry all of these or the greps must be re-targeted:

| phrase | in `SKILL.md` | pinned by |
|---|---|---|
| `PHASE BY PHASE` | :60 (heading — survives) | `evals/conformance.sh:85` (C8) |
| `baseRef=head` | :106 (**inside range**) | `evals/conformance.sh:106-108` (C9) |
| `commit-before-dependent-dispatch` | :116 (**inside range**) | `evals/conformance.sh:106-108` (C9) |
| `phase-batch boundaries` | :125 (**inside range**) | `evals/conformance.sh:88` (C8) |

**Mermaid survives.** `skills/implement/SKILL.md` has **two** ` ```mermaid ` blocks, at :38 (Flow) and :74 (phase walk). Extracting 74-131 removes the second; the `:38` Flow diagram satisfies `evals/conformance.sh:540-562`.

**The real hazard, and the mitigation.** A stub carrying four pinned phrases keeps C8 and C9 green while the behaviour moves behind a pointer. `evals/conformance.sh:83` says C8 exists for *"Issue 1 fix: no single-implementer collapse"*, and `scripts/gate-write.sh:88-90` records a **measured 69% bypass (11/16 tasks)** for a structurally identical "the model was supposed to do X" assumption. The stub **must** carry a hard precondition — *"`plan.md` exists → Read `references/orchestration.md` BEFORE dispatching"* — and a new conformance assertion must pin that sentence, or three deterministic checks become phrase-matching theatre.

**Effect.** 5,289 B × N implementer preloads. On a 5-implementer plan, ~26 KB ≈ 6.6 K input tokens (**unmeasured estimate**, bytes/4). The main-thread rail costs **≤1 copy per session**, not per task: [skills.md](https://code.claude.com/docs/en/skills) states that re-invoking a skill with identical rendered content adds a short "already loaded" note (v2.1.202+). Real, but small in dollars.

**Effort** S mechanical + M for the precondition design. **Risk** as above. Lint: 199 → ~145 of the 210 budget at `scripts/lint-prompt-length.sh:17`.

**Verify.** `bash evals/conformance.sh` → C8, C9 and the mermaid check green, total 209+1 passed. `grep -c '' skills/implement/SKILL.md` < 210.

---

### 6. Free byte reclamation + close the linter loophole

**Change.** (a) Unpad the markdown rules table in `agents/claudehut-brainstormer.md` (2,137 B, line count unchanged at 87). (b) Add a per-file **byte** budget beside the line budgets at `scripts/lint-prompt-length.sh:17-18`, checked inside `lint_file` at :26-33, seeded from *post-unpad* sizes with headroom.

**Files.** `agents/claudehut-brainstormer.md`, `scripts/lint-prompt-length.sh`.

**Why.** §0 cost #5. No prettier/husky/editorconfig exists in the repo, so nothing will re-pad automatically — but nothing stops a human either, and the linter cannot see it.

**Effect.** 2,137 B per brainstormer dispatch, one per full-tier task (`skills/brainstorm/SKILL.md:43`). Small; listed because the unpad is free and the linter change is what stops recurrence.

**Effort** S. **Risk** the byte budget is the risky half. **Correction to one proposal:** `scripts/lint-prompt-length.sh` **is** part of the counted baseline — `evals/conformance.sh:537-538` runs it as an assertion and :535-536 runs its `--self-test`. So a byte budget that flags any existing file flips a conformance pass to a fail. Add a byte-path case to `self_test()` (:45-68), or the new code path ships untested inside a counted assertion.

**Verify.** `bash scripts/lint-prompt-length.sh` → exit 0; `bash evals/conformance.sh` unchanged.

---

### 7. Register `SubagentStart` — observe first, delete prose later

**Change (v0.10 scope: commit 1 only).** Add a `SubagentStart` block to `hooks/hooks.json` running a new `scripts/dispatch-context.sh` that assembles the round-invariant auditor payload (review-rigor verbatim, the enforcement set, capped learnings, LANGUAGE.md, suspects.jsonl) into `hookSpecificOutput.additionalContext`. **Keep the prose at `skills/review/SKILL.md:78-99` in place for v0.10.** Deleting it is a v0.11 commit, gated on an observed payload.

**Files.** `hooks/hooks.json`, `scripts/dispatch-context.sh` (new).

**Depends on item 4.**

**Why.** [hooks.md:836,870](https://code.claude.com/docs/en/hooks): SubagentStart `additionalContext` is injected *"at the start of the conversation, before the first prompt"* of the subagent — so the payload lands in the auditor. `hooks/hooks.json` registers no SubagentStart. `skills/review/SKILL.md:78-99` makes the main thread hand-generate five invariant blocks per auditor.

**Matcher is the spine.** [hooks.md:2006](https://code.claude.com/docs/en/hooks): the matcher value is the plugin-scoped identifier and *"the colon places a plugin-scoped name on the regular-expression path, so anchor the matcher with `^` and `$`"*. Use `^claudehut:claudehut-(reviewer|security-auditor|perf-reviewer|db-reviewer|observability-reviewer|contract-reviewer)$`.

**Why deletion is deferred.** [hooks.md:841](https://code.claude.com/docs/en/hooks): *"If a value exceeds 10,000 characters, Claude Code writes the full text to a file in the session directory and passes Claude the file path with a short preview instead."* A realistic assembly is review-rigor 2,911 B + LANGUAGE.md + enforcement set + suspects + `--top 8` learnings; on a mature store that plausibly crosses 10 k, at which point the "verbatim contract" silently degrades to a file path — the exact failure the design prevents. Item 4 is the mitigation and must land first. Also note [hooks.md](https://code.claude.com/docs/en/hooks) warns that `additionalContext` framed as imperative system instructions can trip prompt-injection defences; `review-rigor.md` is imperative.

**Effect.** Once the prose is deleted (v0.11), the main thread stops **generating** ~5 KB of invariant payload per auditor per round — output tokens, the expensive side, and named subagents each get their own prompt cache so none of it amortises today. **Unmeasured.** The "18 copies" figure circulating is inflated: `skills/review/SKILL.md:144` re-dispatches only auditors owning a surviving `✗`.

**Effort** M. **Risk** the failure is silent — SubagentStart is context-only with no error surface and `evals/conformance.sh:195-197` (E3) greps skill *text*, so CI stays green whether or not the hook fires. That is precisely why v0.10 keeps the prose. Emit an explicit *"enforcement set empty (fast-lane tier)"* line rather than nothing, so a no-op hook cannot read as "nothing to cover".

**Verify (v0.10).** `bash evals/conformance.sh` unchanged (E3 still green — prose intact). Dump a real SubagentStart payload from one review run and confirm the delivered `agent_type` and that the hook fired. Item 3 landing first means a live plugin `agent_type` has already been observed once.

---

### 8. Gate the three mutually-exclusive architecture rules

**Change.** Add an `arch=` axis to `ACTIVE` at `bin/claudehut-init:120` and tag `templates/rules/architecture/{ddd,hexagonal,cqrs}.md` with `stack: arch=<style>` so the gate at `bin/claudehut-init:127-130` skips the inactive two instead of falling through to `cp -f` at :138. **If no trustworthy detector exists, emit none by default** and add on request — strictly better than emitting all three.

**Files.** `bin/claudehut-init`, the three architecture templates, `evals/init-tests.sh`.

**Why.** Measured: 13 templates glob `**/*.java` (40,350 B), **none** carries a `stack:` key; the three architecture styles are 9,385 B of contradictory guidance loaded together in every Java-editing context. `evals/EVAL-REPORT.md:123-134` records the live A/B probe: a `paths:` rule fires 3/3 on edit, 0/3 on create.

**Reframing.** This is a **correctness/quality** item with a cost side-effect, not a cost item — one proposal filed it cost-first while its own headline argument was contradictory guidance. Judge it on the contradiction.

**Effect.** ~6.2 KB of the 9,385 B removed per Java-editing context. Per-task multiplier (main thread + N implementer worktrees + auditors) is **UNMEASURED**.

**Effort** M — blocked on a design call about the detector. **Risk** guessing wrong silently deletes the right rule, hence emit-none-by-default. `bin/claudehut-init:131` skips existing destinations unless `RULES_REFRESH`, so installed projects are unaffected until refresh. The `stack:` gate does substring matching on a space-padded `ACTIVE` string — an `arch=` value must not collide with an existing tag token. Leave the other 10 `**/*.java` rules alone; they are not mutually exclusive, and `templates/rules/security/deserialization.md` in particular should not lose reach to save bytes.

**Verify.** Add gated-out assertions naming ddd/hexagonal/cqrs to `evals/init-tests.sh` (additive: 42 → 42+N). Never ship a new axis untested.

---

### 9. Exempt documentation writes from the write gate

**Change.** Extend the exempt `case` at `scripts/gate-write.sh:38-39` with a narrow docs pattern (`*.md`/`*.txt` **not** under `src/`). **Depends on item 2** (same file, same normalisation helper).

**Files.** `scripts/gate-write.sh`, `evals/gate-tests.sh`.

**Why.** Reproduced: an `Edit` to `README.md` at `complexity=trivial` returns `permissionDecision: deny` with *"run claudehut:discover first"* — the exempt list is exactly `*/.claude/claudehut/*|*/test/*|*Test.java|*IT.java`, with no docs pattern.

**One line, not a `docs` profile.** One proposal wanted a `docs` profile plus a `gate-done.sh` branch plus a conditional test-evidence regex. **I disproved the premise.** I ran `gate-done.sh` against `{"phase":"discover","review":"pending","complexity":"trivial","reuse_scan":false}` — it **exits 0 with no block**, because `scripts/gate-done.sh:39-45` computes `engaged=false` and `:45` returns before the `review != pass` check at :69-70 is ever evaluated. Control case: flipping `reuse_scan=true` *does* block. So the only thing forcing a doc-only session to engage is the write-gate deny itself. Remove that and the whole ceremony disappears. The `docs` profile, the gate-done branch and the `bin/claudehut-state:257-258` conditional are machinery for a state the code never reaches — and `bin/claudehut-state:308-309` defines profile as *"the deliverable rail + which auditors are mandatory"*, neither of which `docs` has.

**Effect.** Removes the entire ceremony for the most common trivial task. The "53,757 B doc-only floor" figure circulating is **unsourced** — I could not locate its derivation. Do not quote it.

**Effort** S. **Risk** the item's own warning is the right one and must survive into the code comment: `scripts/gate-write.sh:43` (`$all_exempt && allow`) short-circuits **before** the fast-lane security/migration check at :76-84, so a loose glob is an evasion path. Anchor the pattern; remember MultiEdit's ALL-must-be-exempt semantics at :35-42.

**Verify.** Eval-neutral before new asserts: every deny assertion in `evals/gate-tests.sh` (:40-47, :111, :117, :124, :130, :135, :177, :224, :228) uses a `.java` path, and `:57` asserts *allow* for a `.md` already under `.claude/claudehut/`. Add asserts for allow-`README.md` and deny-`src/main/java/X.java`.

---

### 10. Ship `.lsp.json` (jdtls) — configuration only

**Change.** Ship `.lsp.json` at the plugin root mapping `.java` → jdtls with `diagnostics: false`, plus the `lspServers` manifest path in `.claude-plugin/plugin.json`.

**Files.** `.lsp.json` (new), `.claude-plugin/plugin.json`.

**Why.** claudehut's reuse detection is grep all the way down — `agents/claudehut-reuse-scanner.md:6` is `tools: Read, Grep, Glob, Write` and `scripts/lint-reuse.sh:51-61` finds duplicate helpers with a `grep -rlE "static …"` regex — on an agent whose own body insists reuse is a judgment, not a grep. [plugins-reference.md](https://code.claude.com/docs/en/plugins-reference) documents `.lsp.json` at the plugin root and `diagnostics` defaulting to `true`, *"Set to `false` to keep code navigation but suppress automatic diagnostic injection"* — important, because PostToolUse already fires on every `Write|Edit` (`hooks/hooks.json`).

**Deliberately excluded: the frontmatter half.** Adding `LSP` to the reuse-scanner's `tools:` requires `background: false` to survive the background filter — and **`background: false` is undocumented**. [sub-agents.md:288](https://code.claude.com/docs/en/sub-agents) documents only *"Set to `true` to always run this subagent as a background task… When unset, Claude chooses, and as of v2.1.198 it runs subagents in the background by default."* There is no documented semantics for `false`. The background allowlist at `sub-agents.md:336` contains no `LSP`, and *"The removal reports no error"*. Do not ship a config whose load-bearing mechanism is inference. Additional unflagged collision: if `background: false` **does** force foreground, it would undo the explorer∥reuse-scanner concurrency formally approved at `.claude/docs/design/11-execution-model-and-artifacts.md:204-220`.

**Effect.** New capability, not a token cut. Plausibly fewer speculative Reads; **unmeasured**.

**Effort** M — the work is validating jdtls against a real Spring repo, not the config. **Risk** jdtls must be installed separately; a Java project's first index is slow (per-session latency, not one-off). Omit `restartOnCrash`/`shutdownTimeout` (v2.1.205+; on earlier versions setting either makes Claude Code skip the server with the reason visible only under `--debug`). Nothing in `evals/` can verify a language server runs.

**Verify.** `claude plugin validate --strict` passes; manual check that `LSP` resolves in a Java project.

---

### 11. Correct the platform-truth prose (all five sites)

**Change.** (a) Replace the false justification *"(a subagent cannot spawn subagents)"* with the correct Law 7 reason (*"runs on the main thread because it owns the `set-review pass` state write"*). (b) Restate "max 3" as a claudehut bench-derived policy, not a platform limit. (c) Add a conformance case asserting every `tools:` entry across `agents/*.md` is on the background allowlist unless the agent sets `background: true`.

**Files.** `skills/review/SKILL.md` (:3 **and** :9), `skills/discover/SKILL.md:13`, `.claude/docs/design/01-agentic-workflow.md:253`, `skills/implement/SKILL.md:103`, `skills/claudehut-workflow/SKILL.md:96`, `evals/conformance.sh`.

**Why.** [sub-agents.md:858](https://code.claude.com/docs/en/sub-agents): *"By default, a subagent can spawn subagents of its own, up to three layers below the main conversation."* The parenthetical is false on current Claude Code. It appears at `skills/review/SKILL.md:9`, `skills/discover/SKILL.md:13` and `.claude/docs/design/01-agentic-workflow.md:253`; the same reasoning is in `skills/review/SKILL.md:3` — the **frontmatter description**, i.e. the always-in-context copy. Fixing only :9 leaves the stale reasoning where the next contributor reads it first. Separately, `skills/implement/SKILL.md:103` presents "max 3" beside "the native concurrency mechanism"; the native cap is 20 (`sub-agents.md`).

**The guard is the real value.** I audited all 14 `tools:` lines — every built-in is on the `sub-agents.md:336` allowlist and MCP tools are always kept, so it ships green. It is what would catch a future agent adding `AskUserQuestion` or `TaskCreate` and silently losing it.

**Effort** S. **Risk** very low. Budget note: `skills/claudehut-workflow/SKILL.md` is 119/120 and `agents/claudehut-reviewer.md` is 90/90 — neither can absorb a line. `skills/review/SKILL.md` is 156/160. The guard is version-coupled; give it a dated comment naming the doc it encodes.

**Verify.** `bash evals/conformance.sh` → +1 assertion, green. `grep -rn 'cannot spawn subagents' skills/ .claude/docs/` → no hits.

---

### 12. Fix the dangling paths in shipped rule templates

**Change.** Rewrite three references inline; add a conformance check that every backticked plugin-relative path in `templates/rules/**` resolves.

**Files.** `templates/rules/framework/flyway-naming.md:87`, `templates/rules/framework/migration-safety.md:109`, `templates/rules/framework/lombok-annotations.md:44`, `evals/conformance.sh`.

**Why.** Verified by scan: `scripts/validate-migration.sh` does not exist (cited twice) and `skills/lombok/assets/templates/lombok.config.tmpl` does not exist — there is no `skills/lombok/`, and C1 pins exactly 10 skills. All three templates are **untagged**, so `bin/claudehut-init:125-140` copies them into every initialized project. Migration safety is exactly where a dangling instruction is most expensive.

**Prefer rewriting to building.** `flyway-naming.md:87` claims *"Phase 5 + PreToolUse hook run `scripts/validate-migration.sh`"* — I read all 79 lines of `hooks/hooks.json`; the PreToolUse entries are `gate-write.sh` and `record-skill.sh` only. The prose is doubly wrong: a missing script **and** wiring that was never built. Fix the sentences; do not build the script to match the fiction.

**Effort** S. **Risk** low — `templates/rules/` is outside both the line budget (`scripts/lint-prompt-length.sh:39-40` covers only `skills/*/SKILL.md` and `agents/*.md`) and the mermaid invariant. **Verify.** `bash evals/conformance.sh` → +1, green.

---

### 13. Correct two false comments in `verify-subagent.sh`

**Change.** Comments only. Do **not** build the marker-sidecar or content-hash rework.

**Files.** `scripts/verify-subagent.sh`.

**Why.** `:38` and `:54-55` both claim the state file is what *"bootstrap.sh wrote at SessionStart, before any subagent"*. The state-file mtime advances across a single `set-profile` call, so the `-newer` comparisons at :43 and :59 mean "since the last state mutation", not "since session start". The codebase already knows mtime is the wrong tool: `bin/claudehut-state:87-89`'s `plan_hash()` comment says *"a content hash beats an mtime compare (immune to same-second edits and equal-mtime clobbers)"*.

**Why comments only.** Neither the proposal nor any verifier reproduced a false block, and the drift is **fail-closed** (a tighter window makes it block more), bounded by the `stop_hook_active` cap at :18. New machinery for an unobserved failure fails the project's own simplicity bar. Note the same pattern lives at `scripts/gate-done.sh:88` (`[ "$artp" -nt "$RECEIPT" ]`) — if it is ever reworked, both sites must move.

**Effort** XS. **Risk** none. **Verify.** No eval pins the mtime semantics; `bash evals/gate-tests.sh` unchanged.

---

### 14. Register `InstructionsLoaded` — ground truth for the enforcement set

**Change.** Register `InstructionsLoaded` against `scripts/record-rules-loaded.sh`, appending each loaded rule path to a sidecar `state/<sid>.rules-loaded`. Review diffs the declared `enforcement_set` against the observed set, **advisory and additive** — the observed set augments, never replaces.

**Files.** `hooks/hooks.json`, `scripts/record-rules-loaded.sh` (new), `skills/review/SKILL.md`.

**Depends on item 1** (the lock helper).

**Why.** The rule plane is path-scoped `.claude/rules/*.md`, but the set Review audits against is read from a **model-authored** manifest at `skills/review/SKILL.md:83`. Nothing observes what actually loaded, so a rule can load, govern the diff, and be absent from the coverage table with every gate green.

**Upgraded to VERIFIED.** One proposal marked the input schema a GUESS; it is published. [hooks.md:1123-1126](https://code.claude.com/docs/en/hooks) documents `file_path` (*"Absolute path to the instruction file that was loaded"*), `memory_type`, `load_reason` (values `session_start`, `nested_traversal`, `path_glob_match`, `include`, `compact`), `globs`, `trigger_file_path`, `parent_file_path`. The jq filter is `.file_path`. **The matcher field is `load_reason`, not `reason`.**

**Safety the proposal missed.** [hooks.md:1106](https://code.claude.com/docs/en/hooks): InstructionsLoaded *"runs asynchronously for observability purposes."* Concurrent async appends to one sidecar is exactly the lost-update shape `963cee8` fixed — reuse the item-1 lock helper. Sidecar only; never the state JSON (Law 7).

**Effect.** Neutral on tokens; buys ground truth. **Effort** M — the hook is small, the Review-side diff is the work, and `skills/review/SKILL.md` has 4 lines of headroom (156/160).

**Verify.** Additive only; collides with no eval. Confirm the sidecar populates during a real session.

---

## 2. Native Claude Code capabilities to adopt

Only VERIFIED items. **Already in use:** 9 hook events (`hooks/hooks.json`), subagent `tools`/`model`/`effort`/`skills`/`memory`/`isolation`, skill `name`/`description`/`allowed-tools`, `${CLAUDE_PLUGIN_ROOT}` substitution.

| Capability | Doc | claudehut change | Plan item |
|---|---|---|---|
| `SubagentStart` + `additionalContext` | [hooks.md:836,870,2006](https://code.claude.com/docs/en/hooks) | Hook-supplied auditor payload; matcher must be `^claudehut:…$` | 7 |
| Plugin-scoped `agent_type` | [hooks.md:2006](https://code.claude.com/docs/en/hooks) | `agent="${agent##*:}"` in `verify-subagent.sh:20` | 3 |
| `InstructionsLoaded` (`file_path`, `load_reason`) | [hooks.md:1123-1126](https://code.claude.com/docs/en/hooks) | Observe which rules actually loaded | 14 |
| `lspServers` / `.lsp.json`, `diagnostics: false` | [plugins-reference.md](https://code.claude.com/docs/en/plugins-reference) | Java symbol navigation instead of regex reuse detection | 10 |
| Nested subagents, default depth 3 | [sub-agents.md:858](https://code.claude.com/docs/en/sub-agents) | Correct the false justification (not to adopt nesting) | 11 |
| Background tool filter | [sub-agents.md:336](https://code.claude.com/docs/en/sub-agents) | Conformance guard on `tools:` entries | 11 |
| **`Stop`/`SubagentStop` `additionalContext`** | [hooks.md:861](https://code.claude.com/docs/en/hooks) | `scripts/gate-done.sh:13` only ever emits `{decision:"block"}`. Every corrective nudge costs a full block-and-retry turn; `additionalContext` is *"non-error feedback that continues the conversation"* — zero extra model calls. **Strong v0.11 candidate**, out of scope here only because it needs a per-message audit of which blocks are genuinely blocking. | — |
| **`claude plugin details`** | [plugins-reference.md:1117-1120](https://code.claude.com/docs/en/plugins-reference) | Prints an **Always-on** vs **On-invoke** token split per component. This turns the entire cost track from hand-counted bytes into a diffable number and belongs in `evals/` as a regression gate. **I could not run it against this checkout** — the installed CLI rejects `--plugin-dir` and the registered copies are disabled — so this is a recommendation, not a measurement I made. | v0.11 |
| `PostToolBatch` `tool_calls` | [hooks.md:1841](https://code.claude.com/docs/en/hooks) | Documented, but see §4 — the right primitive for task bookkeeping is `TaskCreated`/`TaskCompleted` | rejected |

**Skill frontmatter fields deliberately not adopted in v0.10:** `model`/`effort` (turn-scoped, not phase-scoped — [skills.md:263](https://code.claude.com/docs/en/skills): *"applies for the rest of the current turn… the session model resumes on your next prompt"* — so a multi-turn Learn phase silently reverts); `paths` (docs describe it only for *automatic* loading and are silent on whether it filters **preloading**, which `agents/claudehut-implementer.md:7-8` depends on); `disable-model-invocation` (see §4).

---

## 3. MCP integration

**Keep the recommend-only model.** `templates/mcp-recommendations.md:1` states *"ClaudeHut ships NO active .mcp.json — MCP is opt-in per project"*, and `evals/conformance.sh:80` actively asserts no `.mcp.json` is shipped. Reversing that is a v0.11 decision, not a v0.10 one.

### Token cost — state it plainly

An explicitly-named MCP tool in a subagent `tools:` allowlist **loads its schema upfront**, because none of the 14 agents lists `ToolSearch` and so no deferral mechanism is available inside the subagent. Today `agents/claudehut-perf-reviewer.md:6` and `agents/claudehut-security-auditor.md:6` each name 8 MCP tools and `agents/claudehut-db-reviewer.md:6` names 2 — these cost **nothing** at present because the servers are not connected and unresolved entries drop silently. Wiring any server in **converts a free entry into a recurring per-dispatch schema cost.** That is the opposite of the v0.9.2 direction, so wire the minimum.

### Verdict on `bin/kafka-mcp`: delete the stub, do not replace it in v0.10

- **The stub is not free.** `bin/kafka-mcp:24-25` prints to stderr and `exit 1`, so anyone who followed `templates/mcp-recommendations.md` sees a permanently failed server in `/mcp`.
- **Its stated justification has lapsed.** The header says a stub ships *"because no production-ready public Kafka MCP exists"*. I fetched [confluentinc/mcp-confluent README](https://raw.githubusercontent.com/confluentinc/mcp-confluent/main/README.md): it exists, is vendor-published, and ships `--allow-tools` filtering (6 occurrences).
- **But swapping it in is a trap.** That README exposes **hyphenated** tool names — `get-consumer-group-lag`, `list-topics` (3 occurrences each) — while `agents/claudehut-perf-reviewer.md:6` and `agents/claudehut-security-auditor.md:6` declare **underscored** ones, and `evals/conformance.sh:128-130` pins the literal string `mcp__kafka__consumer_group_lag` in both. Pointing `kafka` at Confluent leaves all 12 entries resolving to nothing — *exactly the C10 failure mode*.

**v0.10 action:** delete `bin/kafka-mcp` and rewrite the kafka row in `templates/mcp-recommendations.md` to name Confluent with a `--allow-tools` pin, but **do not rename the frontmatter entries and do not touch `evals/conformance.sh:128-130`.** That rename is a v0.11 commit with its own eval change. Deleting the stub alone removes the `/mcp` failure noise and touches no eval.

### Correct two stale catalog rows (doc-only, breaks nothing)

I fetched [modelcontextprotocol/servers-archived](https://raw.githubusercontent.com/modelcontextprotocol/servers-archived/main/README.md): *"⚠️ IMPORTANT: This repository is archived and no longer maintained"* and *"NO SECURITY GUARANTEES"*, with PostgreSQL, GitHub and Redis in the archive list. So:

- **postgres** — `templates/mcp-recommendations.md:20` emits `npx -y @modelcontextprotocol/server-postgres`, an archived package pointed at a developer's live database. Retarget to [crystaldba/postgres-mcp](https://raw.githubusercontent.com/crystaldba/postgres-mcp/main/README.md), which I fetched and confirmed exposes **underscored** names (`explain_query`, `list_objects`, `analyze_query_indexes`) — no rename churn — and `--access-mode=restricted`. **That flag is load-bearing:** omit it and `execute_sql` becomes read/write, breaking the read-only commitment at `.claude/docs/design/08-mcp-integration.md §6.1`.
- **redis** — archived and unreachable (no agent declares `mcp__redis__*`). **Delete the row.**

### Drop, because no agent can call them

`redis`, `memory`, `github` and `context7` appear in the catalog and in **zero** agent `tools:` lines. Delete the redis and memory rows (memory also duplicates the committed, human-inspectable `.claude/claudehut/` store — a second machine-local memory splits the source of truth, and `.claude/docs/design/08-mcp-integration.md §2.2` must be removed in the same change). Leave github/context7 as documented suggestions or wire them properly in v0.11 — not both half-done.

### Also fix, cheaply

- `templates/mcp-recommendations.md:13-14,20-24` tells developers to paste literal secrets into `--scope project` commands, i.e. into a committed `.mcp.json`, while `.claude/docs/design/08-mcp-integration.md §6.2` then worries about that exact hazard. Use `${VAR}` expansion so the committed file holds no secret, and the manual check can be deleted.
- Add a size caution to the catalog (not to the agent bodies, which are line-budgeted): MCP tool output is capped at 25,000 tokens by default. `mcp__kafka__peek_messages` and an unbounded `SELECT` can trivially hit it — a larger context event than everything v0.9.2 saved.
- Add an **Sentry** row (`https://mcp.sentry.dev/mcp`, OAuth) as a *suggestion only*. `agents/claudehut-observability-reviewer.md:6` is `Read, Grep, Glob` — the only reviewer with an obvious live-data counterpart and no MCP tools. Honest caveat: Sentry describes production errors from shipped code, so it grounds *"is this instrumented well enough"*, not *"is this diff correct"*.

### A closed door — record it so it is not re-proposed

Per-agent `mcpServers` frontmatter would be the ideal way to give one auditor a connection the main session does not pay for. [sub-agents.md:285](https://code.claude.com/docs/en/sub-agents) states it is **"Ignored for plugin subagents."** A v0.10 that added it would look correct in review, pass every eval, and do nothing at runtime. Same for `hooks` (:286) and `permissionMode`.

---

## 4. Deferred and rejected

**Refuted in verification:**

- **`PostCompact` hook to repair a "compaction regression".** `hooks/hooks.json:5` **already** matches `startup|resume|clear|compact`, and I ran `bootstrap.sh` with `source=compact`: it emits **4,374 B** containing the triage block (4 matches for `set-complexity`/`Phase 0`/`triage`). The diagnosis is wrong and the mechanism is redundant. *Residual, much smaller:* only the dynamic off-path `find` at `scripts/inject-phase.sh:42-47` fails to re-run post-compaction — one line in `bootstrap.sh` truncating the `injected-phase` sidecar fixes it, no new hook.
- **Narrowing SessionStart to `startup|resume|clear`.** Would *create* the loss the same proposal wanted to fix, and additionally drop the learnings snapshot (`bootstrap.sh:66-70`), the Summer KB block (:114-118) and the snapshot-restore path (:35-38).
- **`background: false` on the reuse-scanner.** Only `background: true` is documented ([sub-agents.md:288](https://code.claude.com/docs/en/sub-agents)); `false` has no documented semantics. The `.lsp.json` half survives as item 10.
- **A `docs` profile + `gate-done` branch + conditional test-evidence regex.** `gate-done.sh:45` short-circuits on `engaged=false` — I ran it and got exit 0 with no block. Collapsed to item 9's one line.
- **A `PostToolBatch` probe.** Its stated open question is already answered: [hooks.md:1841](https://code.claude.com/docs/en/hooks) documents `tool_calls` with per-call `tool_name`. And `hooks.md:1830` notes *"There is no matcher for this event"*, so registering it spawns a process after every parallel batch in every session. The right primitive for the `TaskUpdate` discipline at `skills/implement/SKILL.md:123-131` is `TaskCreated`/`TaskCompleted`, which are plugin-valid and support exit-code-2 blocking.
- **A Law 7 `agent_id` guard + conformance rule.** Zero reachable violations: all 14 agents carry explicit `tools:` allowlists and **none** grants `Skill`, so `record-skill.sh` cannot fire in a subagent. The proposed general-form assertion would also fail on `scripts/record-skill-expansion.sh:41-42` on day one. Keep one sentence in `.claude/docs/design/06-hooks.md` noting the invariant currently holds by accident.
- **NUL bytes as a *correctness* defect.** Real reviewability problem (`git show --stat 963cee8` renders `Bin 16902 -> 17654 bytes` — the concurrency fix shipped with an empty diff), and I confirmed the NULs (`merge-learnings.sh` ×1, `record-failure.sh` ×2). But bash strips NUL when **reading script source**, so *both* separators are inert and both comparison sites compute both sides identically — there is no behavioural defect. Ship it as reviewability + a byte-scan conformance guard, not as a bug fix. *(Kept out of the numbered plan: valuable but not load-bearing for v0.10.)*
- **Draining-sidecar for per-prompt learnings.** Would permanently suppress a pitfall that becomes relevant at prompt 20 because it surfaced at prompt 3; on a 20-50 record store the pool empties within ~5 prompts, silently disabling the mechanism. Item 4 ships the safe half. *(The "same records recycle all session" claim was also unlabelled inference — `inject-phase.sh:56` passes `--filter "$prompt"`, so a different prompt selects a different subset.)*
- **`verify-subagent.sh` marker-sidecar / content-hash rework.** Consequence never reproduced by anyone; comments-only fix ships as item 13.

**Prior-report §3 items the post-0.9.2 measurements no longer justify:**

- **Thinning the ~100 KB `skills/implement/references/` playbook pool.** It is on-demand behind the routing table at `skills/implement/SKILL.md:155-167`; `evals/EVAL-REPORT.md:176-196` measured 13/15 reads with 0 path errors, so a task reads 1-3 files. The recurring cost is the 16,213 B **index** (item 5). Acting here would be actively harmful: one of the two measured misses shipped an actual `.anyRequest().permitAll()`.
- **Artifact double-reads between phases.** Mostly phantom — every `bin/claudehut-state` validator is a shell grep or shasum costing 0 model tokens (:145-146, :174-177, :253-265). Only `reuse-scan.md` is a genuine ~2-4 KB double-pay, and having the scanner self-report weakens the independent DECISION check for 2 KB.
- **Native `memory: project` replacing the learnings plane.** `sub-agents.md:521` injects up to 200 lines / 25 KB of `MEMORY.md` per dispatch — swapping a ~1.5 KB capped injection for a 25 KB one is a cost regression, and it silently no-ops when `autoMemoryEnabled` is false.
- **`disable-model-invocation` on the phase skills.** [skills.md:259](https://code.claude.com/docs/en/skills) confirms it *"Also prevents the skill from being preloaded into subagents"*, which kills `skills: [implement]` at `agents/claudehut-implementer.md:7-8` and the TDD Iron Law inside every implementer (breaking conformance G4/G5), **and** blocks programmatic Skill-tool invocation, which breaks the REQUIRED-NEXT chain asserted by C3 at `evals/conformance.sh:22-30`. Only `skills/claudehut-init/SKILL.md` is safe, recovering ~299 B — not worth the footgun beside it.
- **`maxTurns` as a cost cap on the auditors.** Documented at [sub-agents.md:283](https://code.claude.com/docs/en/sub-agents) as *"Maximum number of agentic turns before the subagent stops"* — and nothing states what the parent receives when it fires. A silently truncated audit would read as a clean pass at the review gate: a **fail-open on the quality gate**. This is a constraint the cost track must respect, not an idea to revisit.
- **A 15th `claudehut-review-lead` agent.** Nesting is real (depth 3), but it breaks C4 (`evals/conformance.sh:37`, exactly 14 agents), needs a mermaid block and a ≤90-line body, and — decisively — the refute pass at `skills/review/SKILL.md:103-111` is *by design* the main thread opening each cited locus itself. A lead validating its children's self-reports is strictly weaker.
- **Plugin `workflows/`.** *"No mid-run user input… For sign-off between stages, run each stage as its own workflow"* kills `write-spec` and `write-plan`, which own `AskUserQuestion` gates. And nothing establishes that plugin `PreToolUse` hooks fire inside `agent()`-spawned workflow agents — if they do not, a workflow bypasses `gate-write.sh` and the Iron Law on a write path. Reject for anything that writes.
- **Agent teams, plugin-shipped `statusLine`, `experimental.monitors`.** Teams are experimental, documented as using *"significantly more tokens"*, and do not apply a subagent's `skills`/`mcpServers` when it runs as a teammate. `statusLine` is a settings key, not a plugin component path. Monitors *"run only in interactive CLI sessions"* and claudehut's evals are headless `-p`.
- **Skill `context: fork`.** Every state-writing phase skill is excluded by Law 7 and `.claude/docs/design/11-execution-model-and-artifacts.md:29` records fork was already *removed* from write-plan and capture-learnings for that reason. A forked skill also cannot use `AskUserQuestion` ([skills.md:552](https://code.claude.com/docs/en/skills) withdraws the fork tool-filter exemption). A new read-only fork skill would be an 11th skill and break C1.
- **Deleting the four `MultiEdit` tests at `evals/gate-tests.sh:214-225`.** Absence from a docs table is not a removal notice, and dropping 4 assertions on a GUESS moves the baseline. Item 2's normalisation preserves all four. *Cheap real resolution:* dump one live MultiEdit `PreToolUse` payload and settle the shape that `scripts/gate-write.sh:22-29` itself flags as unconfirmed.
- **Adding `Bash` to the PreToolUse matcher.** Already a documented limitation at `.claude/docs/design/06-hooks.md:166`; gating it needs sound shell parsing and would fail open where it matters.

---

## 5. Risks and constraints this plan must respect

**Eval pins (breaking these is a red build):**

| Constraint | Location |
|---|---|
| Exactly **10** skill directories (`ls -1d skills/*/`) | `evals/conformance.sh:18-19` |
| Exactly **14** agents | `evals/conformance.sh:37` |
| Non-empty ` ```mermaid ` block in **every** `agents/*.md` and `skills/*/SKILL.md` | `evals/conformance.sh:540-562` |
| C8: `PHASE BY PHASE` + `phase-batch boundaries` in `implement/SKILL.md` | `evals/conformance.sh:85,88` |
| C9: `baseRef=head` + `commit-before-dependent-dispatch` in `implement/SKILL.md` | `evals/conformance.sh:106-108` |
| C10: literal `mcp__kafka__consumer_group_lag` in perf-reviewer **and** security-auditor | `evals/conformance.sh:128-130` |
| C10: `mcp__postgres__list_tables`/`describe_table` are **banned** (Resources, not Tools) | `evals/conformance.sh:116-121` |
| No shipped `.mcp.json` | `evals/conformance.sh:80` |
| E3: `review-rigor` **and** `verbatim` in `review/SKILL.md`; `Standards` in `review-rigor.md` | `evals/conformance.sh:195-197` |
| G2/G3: `ultrathink` + `Fit` in reuse-scanner; `effort: high` in its frontmatter | `evals/conformance.sh:203-210` |
| G4/G5: `ultrathink` + `design beat` in `implement/SKILL.md` | `evals/conformance.sh:214-219` |
| C3: the REQUIRED-NEXT phase chain, skill by skill | `evals/conformance.sh:22-30` |
| WS-9: `lint-prompt-length.sh` **and** its `--self-test` are *counted conformance assertions* | `evals/conformance.sh:535-538` |

**Line budgets** (`scripts/lint-prompt-length.sh:17-18`) — the linter measures `grep -c ''`, i.e. **lines**, so intra-line whitespace is free:

| file | now | cap | headroom |
|---|---|---|---|
| `skills/review/SKILL.md` | 156 | 160 | 4 |
| `skills/implement/SKILL.md` | 199 | 210 | 11 |
| `skills/claudehut-workflow/SKILL.md` | 119 | 120 | **1** |
| `agents/claudehut-reviewer.md` | 90 | 90 | **0** |
| `agents/claudehut-reuse-scanner.md` | 104 | 105 | 1 |
| `agents/claudehut-brainstormer.md` | 87 | 90 | 3 |

The linter also bans provenance tags (`RC-x`, `Issue-N`, `EVAL-REPORT`, `measured N/M`) from always-loaded bodies — so none of this document's citations may be pasted into a skill or agent body.

**Architectural:**

- **Law 7** — only the main thread writes `.claude/claudehut/state/<sid>.json`; hook bookkeeping goes in sidecars (`scripts/inject-phase.sh:21` is the pattern). Item 14's sidecar is written by an **async** hook ([hooks.md:1106](https://code.claude.com/docs/en/hooks)) and needs item 1's lock.
- **Plugin subagents ignore `hooks`, `mcpServers`, `permissionMode`** — [sub-agents.md:285-286](https://code.claude.com/docs/en/sub-agents). `once: true` is therefore only placeable in skill frontmatter.
- **`scripts/gate-write.sh:43`** (`$all_exempt && allow`) short-circuits *before* the fast-lane security/migration check at :76-84 — anything added to the exempt list bypasses that check entirely.
- **`set -euo pipefail`** at `scripts/gate-write.sh:8`: a non-zero helper kills the hook, which emits nothing, which Claude Code treats as **allow**.
- **CI runs 6 of 8 suites** (`.github/workflows/ci.yml:59-77`). Prefer **per-suite floors** to a global integer, and add `reference-check.sh` + `worktree-tests.sh` while you are there.
- **Platform split:** CI ubuntu-latest has `flock`; macOS does not and takes the mkdir branch. Concurrency tests validate a different path per platform.
- **`skills/implement/SKILL.md` is preloaded** into the implementer — every byte is paid per dispatch.
- **`hooks/hooks.json` matches `MultiEdit`**, a tool absent from the current tools reference. Matcher lists treat unmatched names as inert, so nothing breaks; do not delete the four tests without a live payload.

---

## 6. Confidence ledger

| Claim | Status | Evidence |
|---|---|---|
| Eval baseline is **406 passed / 2 failed**, not 410/2 | VERIFIED | Ran all 8 suites at HEAD: 207+2F, 77, 42, 8, 32, 8, 18, 14 |
| Both failures come from untracked `skills/fable5-thinking/` | VERIFIED | `git status --porcelain` → only `?? skills/fable5-thinking/`; `evals/conformance.sh:18-19` + `:540-562` |
| Post-prerequisite baseline **408/0** | PLAUSIBLE | 406 + 2 recovered failures; not re-run after removing the stub |
| CI runs only 6 of 8 suites | VERIFIED | `.github/workflows/ci.yml:59-77`; no `reference-check`/`worktree-tests` |
| `bin/claudehut-state` loses updates: **15/15** | VERIFIED | Raced `set-complexity` vs `set-profile` 15× from a common base |
| The `set-phase`-vs-`mark-skill` probe proves nothing | VERIFIED | `bin/claudehut-state:135` resets `implement_skill_ok=false` on `set-phase discover\|brainstorm` |
| PreToolUse hook timeout default is **600 s**, not 30 | VERIFIED | [hooks.md](https://code.claude.com/docs/en/hooks); only UserPromptSubmit (30) and MessageDisplay (10) are lowered |
| Package-named-`test` prod file → **ALLOW** | VERIFIED | Ran `gate-write.sh` on `$PROJECT_DIR/src/main/java/com/acme/test/Pay.java` → empty output |
| Traversal string → **ALLOW** | VERIFIED | Ran on `/etc/.claude/claudehut/../../../src/main/java/Evil.java` → empty output |
| `README.md` prod write → **DENY** | VERIFIED | Ran `gate-write.sh`, got `permissionDecision: deny`, *"run claudehut:discover first"* |
| Unengaged doc-only session → `gate-done.sh` **exits 0** | VERIFIED | Ran with `reuse_scan=false` → exit 0; with `true` → block. `gate-done.sh:39-45` |
| Exactly **one** existing gate-test breaks under item 2 (`:221`, not `:57`) | PLAUSIBLE | Verifier ran the patched script (76/1); I confirmed the `:57` mechanism by reading `evals/gate-tests.sh:41-57` but did not re-run the patch |
| `skills/implement/SKILL.md` = **16,213 B**; lines 74-131 = **5,289 B** | VERIFIED | `wc -c`; `sed -n '74,131p' \| wc -c` |
| Four pinned phrases inside/at the extraction range | VERIFIED | `grep -n` → :60, :106, :116, :125; `evals/conformance.sh:85,88,106-108` |
| Two mermaid blocks in `implement/SKILL.md` (:38, :74) | VERIFIED | `grep -n '```mermaid'` |
| `skills:` preloads the **full** skill body | VERIFIED | [sub-agents.md:284](https://code.claude.com/docs/en/sub-agents) |
| Item 5 saves ~26 KB ≈ 6.6 K tokens on a 5-implementer plan | UNMEASURED-ESTIMATE | 5,289 B × 5, bytes/4; N is task-dependent |
| Main-thread skill rail costs ≤1 copy per session | VERIFIED | [skills.md](https://code.claude.com/docs/en/skills), v2.1.202 "already loaded" note |
| `review-rigor.md` = **2,911 B**, required verbatim per code-review dispatch | VERIFIED | `wc -c`; `skills/review/SKILL.md:81` |
| Item 7's payload saving | UNMEASURED | Not measured; "18 copies" is inflated per `skills/review/SKILL.md:144` |
| `additionalContext` >10,000 chars spills to a file path | VERIFIED | [hooks.md:841](https://code.claude.com/docs/en/hooks) |
| SubagentStart injects at the start of the subagent's conversation | VERIFIED | [hooks.md:836,870](https://code.claude.com/docs/en/hooks) |
| Plugin `agent_type` is plugin-scoped; anchor the matcher | VERIFIED | [hooks.md:2006](https://code.claude.com/docs/en/hooks) |
| `verify-subagent.sh` switches on **bare** names; only consumer of `agent_type` | VERIFIED | `scripts/verify-subagent.sh:20,24,29,34,48,65-67`; repo-wide grep |
| Item 3 leaves all 8 existing assertions green | PLAUSIBLE | Verifier measured 77/0 and 207/2 on a patched copy; I did not re-run the patch |
| `bootstrap.sh` on `source=compact` emits **4,374 B** incl. triage | VERIFIED | Ran the real hook; 4 grep matches for `set-complexity`/`Phase 0`/`triage` |
| `hooks/hooks.json:5` already matches `compact` | VERIFIED | Read all 79 lines of `hooks/hooks.json` |
| Prior 7,704 B SessionStart figure | UNMEASURED-ESTIMATE | Measured against a 20-record synthetic store, not this repo |
| **13** rule templates glob `**/*.java` = **40,350 B**, none tagged `stack:` | VERIFIED | Frontmatter parse over `templates/rules/**/*.md` |
| ddd 3,328 + hexagonal 3,811 + cqrs 2,246 = **9,385 B** | VERIFIED | `os.path.getsize` on each |
| `paths:` rules fire 3/3 on edit, 0/3 on create | VERIFIED (in-repo) | `evals/EVAL-REPORT.md:123-134` (live A/B probe, prior measurement) |
| Per-task multiplier for those rules | UNMEASURED | Load frequency per context not probed |
| **2,137 B** of table padding in `claudehut-brainstormer.md` (2,164 corpus) | VERIFIED | Re-measured: squeeze interior spaces on `^\s*\|` rows across all agents + skills |
| `a458e6d` grew that file 1,933 B while lines *fell* 9 | PLAUSIBLE | Verifier ran `git show a458e6d~1:` vs `a458e6d:`; I confirmed only the current 87-line state |
| `lint-prompt-length.sh` is a **counted** conformance assertion | VERIFIED | `evals/conformance.sh:535-538` |
| `inject-learnings.sh` defaults `MAXLEN=0`; `review/SKILL.md:85` is the sole uncapped caller | VERIFIED | `scripts/inject-learnings.sh:14`; `skills/review/SKILL.md:85`; `bootstrap.sh:68`; `inject-phase.sh:56` |
| Item 4's saving | UNMEASURED-ESTIMATE | Entirely store-dependent; ~0 on crisp records |
| `background: false` is **undocumented** | VERIFIED | [sub-agents.md:288](https://code.claude.com/docs/en/sub-agents) documents only `true` |
| Background allowlist excludes `LSP`; removal reports no error | VERIFIED | [sub-agents.md:336](https://code.claude.com/docs/en/sub-agents) |
| All 14 agents currently survive the background filter | PLAUSIBLE | Two researchers audited all `tools:` lines; I spot-checked, did not re-audit all 14 |
| Nested subagents default to depth 3 | VERIFIED | [sub-agents.md:858](https://code.claude.com/docs/en/sub-agents) |
| "cannot spawn subagents" appears at 3 sites + the description | VERIFIED | `skills/review/SKILL.md:3,9`; `skills/discover/SKILL.md:13`; `.claude/docs/design/01-agentic-workflow.md:253` |
| "max 3" appears at 2 skill sites | VERIFIED | `skills/implement/SKILL.md:103`; `skills/claudehut-workflow/SKILL.md:96` |
| `InstructionsLoaded` input has `file_path` / `load_reason` | VERIFIED | [hooks.md:1123-1126](https://code.claude.com/docs/en/hooks) |
| `InstructionsLoaded` runs **asynchronously** | VERIFIED | [hooks.md:1106](https://code.claude.com/docs/en/hooks) |
| `Stop`/`SubagentStop` accept `additionalContext` | VERIFIED | [hooks.md:861](https://code.claude.com/docs/en/hooks) |
| `PostToolBatch` receives `tool_calls`; no matcher | VERIFIED | [hooks.md:1830,1841](https://code.claude.com/docs/en/hooks) |
| `claude plugin details` reports Always-on / On-invoke tokens | VERIFIED (doc only) | [plugins-reference.md:1117-1120](https://code.claude.com/docs/en/plugins-reference) — **I could not run it here**: the CLI rejects `--plugin-dir` and registered copies are disabled |
| Confluent Kafka MCP uses **hyphenated** tool names + `--allow-tools` | VERIFIED | Fetched [README](https://raw.githubusercontent.com/confluentinc/mcp-confluent/main/README.md): `get-consumer-group-lag` ×3, `list-topics` ×3, `--allow-tools` ×6 |
| `@modelcontextprotocol/server-postgres` / `-github` / `-redis` archived, "NO SECURITY GUARANTEES" | VERIFIED | Fetched [servers-archived README](https://raw.githubusercontent.com/modelcontextprotocol/servers-archived/main/README.md) |
| crystaldba/postgres-mcp uses **underscored** names + `--access-mode=restricted` | VERIFIED | Fetched [README](https://raw.githubusercontent.com/crystaldba/postgres-mcp/main/README.md): `explain_query`, `list_objects`, `analyze_query_indexes`, `access-mode=restricted` |
| `evals/conformance.sh:128-130` pins the literal `mcp__kafka__consumer_group_lag` | VERIFIED | `grep -n` |
| `bin/kafka-mcp` `exit 1`s as a documented stub; justification lapsed | VERIFIED | `bin/kafka-mcp:1-25` (header claims no public server exists; Confluent does) |
| Named MCP tools load schemas upfront in subagents | PLAUSIBLE | Researcher probe used a **project** subagent, not a plugin one; docs do not distinguish |
| Unresolved MCP entries drop silently | PLAUSIBLE | Same probe caveat; consistent with [sub-agents.md](https://code.claude.com/docs/en/sub-agents) on zero-resolving `tools` |
| Plugin subagents ignore `mcpServers`/`hooks`/`permissionMode` | VERIFIED | [sub-agents.md:285-286](https://code.claude.com/docs/en/sub-agents) |
| `maxTurns` truncation semantics are undocumented | VERIFIED | [sub-agents.md:283](https://code.claude.com/docs/en/sub-agents) is the only substantive mention |
| `disable-model-invocation` blocks preload **and** programmatic invocation | VERIFIED | [skills.md:259](https://code.claude.com/docs/en/skills) |
| NUL bytes in exactly 2 scripts (1 + 2 occurrences) | VERIFIED | Byte scan over `scripts/*.sh` + `bin/*` |
| NULs are behaviourally **inert** (bash strips at source read) | PLAUSIBLE | Verifier measured on bash 3.2 (macOS); not confirmed on CI's bash 5 |
| 3 dangling template paths / 2 missing targets | VERIFIED | `grep -rn` + `ls`: no `scripts/validate-migration.sh`, no `skills/lombok/` |
| No PreToolUse hook runs `validate-migration.sh` | VERIFIED | Read all 79 lines of `hooks/hooks.json` |
| State-file mtime advances on any `set-*` call | PLAUSIBLE | Verifier measured `1785269733 → 1785269961` across one `set-profile`; mechanism visible at `bin/claudehut-state:338-343` |
| `verify-subagent.sh` false-block consequence | UNMEASURED | Not reproduced by any party — hence comments-only in item 13 |
| "53,757 B doc-only floor" | UNMEASURED-ESTIMATE | Unsourced; do not quote |
| Line budgets / current line counts in §5 | VERIFIED | `wc -l` / `grep -c ''` per file; `scripts/lint-prompt-length.sh:17-18` |
