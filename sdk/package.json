{
  "name": "claudehut-sdk-orchestrator",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Phase 7.1 — a programmatic ClaudeHut orchestrator on the Claude Agent SDK. Wraps the bash plugin runtime (state machine, dispatch-prompt enrichment, parallel build workers) behind a deterministic JS control loop.",
  "bin": {
    "claudehut-orchestrate": "./orchestrator.mjs"
  },
  "scripts": {
    "gen-config": "bash gen-agent-config.sh",
    "test": "node --test test/"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.158"
  }
}
