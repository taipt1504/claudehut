# Test runs / scratch
tests/e2e/.runs/
tests/.tmp/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*~

# Session-local Claude state (from running claude inside plugin dir)
.claude/

# eval harness: keep committed reference rows, ignore nothing by default (rows are small evidence)
