Implement `com.example.TextUtils.slugify(String)` so it produces a URL slug
following **the project's established slug convention**. The method currently
throws `UnsupportedOperationException`.

The slug must match what this project already does elsewhere — consult project
memory for the convention rather than assuming a generic one. Implement it and
make sure the behavior is correct.
