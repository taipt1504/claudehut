Add a `SumService` with a method `sum(List<Integer>)` that returns the total, built test-first.

This is a clean first run: ClaudeHut should move through Brainstorm → Spec → Plan → Implement → Review → Learn, write a failing test before the implementation, and end with a Learn pass.
