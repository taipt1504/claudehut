Add URL-slug generation for article titles (e.g. an endpoint or service that turns a title into a slug).

A reusable `TextUtils.slugify(String)` already exists in this project. ClaudeHut's reuse-scan (the Brainstorm phase) should find it and the chosen approach should ADOPT it — do not write a second slug implementation.
