{
  "note": "Checked-in snapshot of every mcp__server__tool named in agents/*.md. reference-check.sh diffs the tree against this list, so ADDING a tool name to an agent fails CI until the name is verified against the server's own docs and recorded here. v0.10 shipped mcp__postgres__query, which does not exist.",
  "verified_against": {
    "postgres": "crystaldba/postgres-mcp (uvx postgres-mcp --access-mode=restricted)",
    "mysql": "mysql MCP server",
    "kafka": "Confluent kafka MCP"
  },
  "tools": [
    "mcp__kafka__consume-messages",
    "mcp__kafka__describe-consumer-group",
    "mcp__kafka__get-consumer-group-lag",
    "mcp__kafka__list-consumer-groups",
    "mcp__kafka__list-topics",
    "mcp__mysql__mysql_query",
    "mcp__postgres__execute_sql",
    "mcp__postgres__explain_query",
    "mcp__postgres__get_object_details",
    "mcp__postgres__list_objects",
    "mcp__postgres__list_schemas"
  ]
}